# Coverage reporting and pre-push build/size checks now run in ci.yml (prepush job).
# This workflow is disabled to avoid duplicate vitest runs on every PR.
name: Test Coverage PR

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Coverage runs in the CI workflow prepush job on pull_request."
