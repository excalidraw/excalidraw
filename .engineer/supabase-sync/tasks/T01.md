# T01 — Config & foundation

## Description
Lay the foundation: add the Supabase dependency, env typings, shared constants, the feature flag, and the
memoized client singleton. **This task owns the shared `app_constants.ts` and `excalidraw-app/package.json`
edits** (collision rule — no other task touches these two files).

## Files
- `excalidraw-app/package.json` — add `"@supabase/supabase-js": "^2.45.0"` to dependencies.
- `excalidraw-app/vite-env.d.ts` — add to `ImportMetaEnv`: `VITE_APP_SUPABASE_URL`, `VITE_APP_SUPABASE_ANON_KEY`, `VITE_APP_FEATURE_SUPABASE_SYNC` (all `string` / `string | undefined` to match file style).
- `excalidraw-app/app_constants.ts` — add `STORAGE_KEYS.LOCAL_STORAGE_SUPABASE_META = "excalidraw-supabase-meta"` and `SUPABASE_SYNC_DEBOUNCE_MS = 2000` (match existing export style; see LLD part1 §B and the reconciliation note that STORAGE_KEYS lives HERE, not localStorage.ts).
- `excalidraw-app/data/supabase/featureFlags.ts` — `export const isSupabaseSyncEnabled = (): boolean => import.meta.env.VITE_APP_FEATURE_SUPABASE_SYNC === "true";`
- `excalidraw-app/data/supabase/client.ts` — `getSupabaseClient(): SupabaseClient | null` memoized singleton; returns `null` if flag off OR url/anon key missing; created with `{ auth: { autoRefreshToken: true, persistSession: true } }`.

## Read first
- `04-lld-part1-data.md` §B (client.ts, featureFlags.ts) and the "Verified real signatures" section.
- `04-lld.md` reconciliation points #1, #2, #6.
- Real files: `excalidraw-app/vite-env.d.ts`, `excalidraw-app/app_constants.ts` (find STORAGE_KEYS), `excalidraw-app/package.json`.

## Acceptance criteria
- `isSupabaseSyncEnabled()` returns true only when the env var is exactly `"true"`.
- `getSupabaseClient()` returns the same instance on repeated calls; returns `null` when flag off or env missing (no throw).
- New env vars are typed in `ImportMetaEnv` (no TS errors referencing them).
- `STORAGE_KEYS.LOCAL_STORAGE_SUPABASE_META` and `SUPABASE_SYNC_DEBOUNCE_MS` exported and importable.
- No behavior change when flag is off.

## Verify
```
yarn test:typecheck
```
(No new test file required for T01; it's foundation. Typecheck must pass.)

## Outcome log
_(implementer appends evidence here)_

## Outcome

### Files modified
- `excalidraw-app/package.json` — added `"@supabase/supabase-js": "^2.45.0"` to `dependencies`, placed alphabetically after `@sentry/browser`. (Note: other deps use pinned versions, but spec + LLD §2 explicitly require the `^2.45.0` caret range, so followed verbatim.)
- `excalidraw-app/vite-env.d.ts` — added `VITE_APP_SUPABASE_URL`, `VITE_APP_SUPABASE_ANON_KEY`, `VITE_APP_FEATURE_SUPABASE_SYNC` to `interface ImportMetaEnv`, all typed `: string;` to match the file's existing style (existing members are plain `VITE_APP_*: string;`, NOT `readonly` — followed the actual file, not the prompt's guess).
- `excalidraw-app/app_constants.ts` — added `SUPABASE_SYNC_DEBOUNCE_MS = 2000` among the top time constants, and `LOCAL_STORAGE_SUPABASE_META: "excalidraw-supabase-meta"` to the `STORAGE_KEYS` object (SCREAMING_CASE key, quoted string value, matching siblings).

### Files created
- `excalidraw-app/data/supabase/featureFlags.ts` — `export const isSupabaseSyncEnabled = (): boolean => import.meta.env.VITE_APP_FEATURE_SUPABASE_SYNC === "true";`
- `excalidraw-app/data/supabase/client.ts` — `export const getSupabaseClient = (): SupabaseClient | null` memoized singleton. Module-level `client: SupabaseClient | null | undefined` (`undefined` = unresolved, `null` = resolved-disabled). Returns cached client if resolved; returns `null` (no throw) when flag off or url/anon key missing (with a one-time `console.warn` when enabled-but-unconfigured); otherwise `createClient(url, anonKey, { auth: { autoRefreshToken: true, persistSession: true } })`. Anon key only — no service-role key referenced.

### Verify result: PASS
`yarn test:typecheck` from repo root — exit code 0, 0 errors. Tail of output:
```
yarn run v1.22.22
$ tsc
Done in 6.65s.
```

### Deviations
- vite-env members are non-`readonly` `: string;` (matched the real file rather than the prompt's `readonly VITE_APP_*` guess; prompt said "follow whatever the file does").
- T01 spec lists client auth options as exactly `{ autoRefreshToken: true, persistSession: true }`; LLD §B.2 additionally suggested `detectSessionInUrl: true`. Followed the T01 task spec (authoritative for this task) and did NOT add `detectSessionInUrl` — flagging for the auth/redirect task if needed.
- Did not implement `resetSupabaseClientForTests` (mentioned in LLD §B.2 but not in the T01 spec's required exports; T01 has no test).
