# T09 — App.tsx sync wiring

## Description
Wire the sync feature into `App.tsx`: construct the Supabase FileManager, mount `useSupabaseSync`, push on
`onChange`, and restructure `renderTopRightUI` (B1) so the sync button renders. **All gated by
`isSupabaseSyncEnabled()`.** This task owns App.tsx *sync* edits (T10 owns the *disable* edits — keep them
separable).

## Files
- `excalidraw-app/App.tsx` only.

## Edits (from `04-lld-part2-ui-tests.md` §C-1, with NORMATIVE corrections)
1. Imports: `isSupabaseSyncEnabled`, `getSupabaseClient`, `createSupabaseFileManager`, `useSupabaseSync`, `SyncStatusButton`, `userIdAtom`/session, `SignInDialog` (+ a `signInOpen` state).
2. Construct the Supabase FileManager (memoized on client+userId) via `createSupabaseFileManager(client, userId)` — **use the factory (M2), not bare `new FileManager(callbacks)`**. Only when flag on + signed in.
3. Mount `const { status, lastSyncedAt, syncNow } = useSupabaseSync({ excalidrawAPI, fileManager })` where `excalidrawAPI` is available.
4. `onChange` (after `LocalData.save(...)`, ~line 689): `if (isSupabaseSyncEnabled()) { /* engine.notifyChange via hook-exposed ref or call syncNow-less notify */ }` — per the hook design, the hook's engine observes scene through `getScene`; expose a `notifyChange` from the hook OR have the hook subscribe. **Simplest per LLD:** add `notifyChange` to the hook's return and call it here. (If the hook instead wraps onChange, follow the hook's actual API from T07 — coordinate.)
5. `renderTopRightUI(isMobile)` B1 restructure (~955-958): evaluate `isSupabaseSyncEnabled()` and `return <SyncStatusButton .../>` **before** the `!collabAPI` early-return. Pass `status,lastSyncedAt,onSyncNow=syncNow,isSignedIn,onRequestSignIn=()=>setSignInOpen(true)`.
6. Render `<SignInDialog open={signInOpen} onClose={()=>setSignInOpen(false)} />` (flag-gated).

## Read first
- `04-lld.md` **NORMATIVE Engine ↔ Hook contract** (esp. how `notifyChange`/hook API works — align with what T07 actually exported), reconciliation #3, #4.
- `04-lld-part2-ui-tests.md` §C-1 (exact anchors + before/after sketches).
- The ACTUAL T07 hook return shape (read `data/supabase/useSupabaseSync.ts` after T07 lands) to call it correctly.

## Acceptance criteria
- With flag ON: the SyncStatusButton renders in the top-right (even though collab is/*will be* disabled and `collabAPI` is null).
- `onChange` triggers the engine's `notifyChange` path (verify via T11/integration or a targeted assertion).
- With flag OFF: `renderTopRightUI` behaves exactly as before (LiveCollaborationTrigger), no SyncStatusButton, no SignInDialog.
- Typecheck passes; existing App tests still pass.

## Verify
```
yarn test:typecheck && yarn vitest run excalidraw-app/tests
```
(Run the app test dir to catch regressions from editing App.tsx. Full suite in REGRESSION.)

## Outcome log
_(implementer appends evidence here)_

## Outcome

**Status: DONE.** All edits in `excalidraw-app/App.tsx` only; additive + flag-gated. Flag-off path is
byte-for-byte the current app (every new branch is under `isSupabaseSyncEnabled()`, and the hook is a
documented no-op when the flag is off / signed-out).

### Contract reconciliation (deviation from the T09.md/§C-1 sketch — IMPORTANT)
The §C-1 / T09.md sketch said to construct a Supabase `FileManager` in `App.tsx` (`useMemo` →
`createSupabaseFileManager`/`new FileManager(...)`) and pass it to the hook as
`useSupabaseSync({ excalidrawAPI, fileManager })`. **That is stale.** The ACTUAL hook
(`data/supabase/useSupabaseSync.ts`, landed by T07) takes **only** `{ excalidrawAPI }` and builds the
FileManager internally (M2). Its `notifyChange()` takes **no args** (reconciliation B2). I followed the
real hook + the prompt's CRITICAL contract facts: **no FileManager is built in App.tsx**, and `onChange`
calls `supabaseSync.notifyChange()` with no args. This matches `04-lld.md` NORMATIVE §B1/B2/M2.

### Edit sites (final line numbers in `excalidraw-app/App.tsx`)
1. **Imports — lines 132-137** (after `./data/tabSync`, before `./share/ShareDialog`):
   `isSupabaseSyncEnabled` (featureFlags), `useSupabaseSync`, `userIdAtom` (sessionAtom),
   `syncStatusAtom` (syncStatusAtom, for the `error` string), `SyncStatusButton`, `SignInDialog`.
   `useAtomValue` / `useState` were already imported (lines 36, 87) — not re-added.
2. **Hook mount + state — lines 419-425** (top level of `ExcalidrawWrapper`, right after the
   `collabError` atom read at :416, where `excalidrawAPI` from :374 is in scope; all hooks
   unconditional per Rules of Hooks):
   ```ts
   const supabaseSync = useSupabaseSync({ excalidrawAPI });
   const supabaseUserId = useAtomValue(userIdAtom);
   const supabaseSyncStatus = useAtomValue(syncStatusAtom);
   const [isSignInOpen, setIsSignInOpen] = useState(false);
   ```
3. **onChange notify — lines 731-736** (after the `LocalData.save(...)` block closes, before the debug
   renderer): `if (isSupabaseSyncEnabled()) { supabaseSync.notifyChange(); }`. The existing
   `collabAPI?.syncElements(...)` (`:682`) and `LocalData.save(...)` (`:689`) are untouched.
4. **renderTopRightUI B1 restructure — lines 977-1002.** Split `isMobile` out to its own
   `return null` (977-979); added the `isSupabaseSyncEnabled()` branch returning `<SyncStatusButton/>`
   (inside the existing `excalidraw-ui-top-right` wrapper) at **985-998 — BEFORE** the original
   `if (!collabAPI || isCollabDisabled) return null;` (now 1000-1002). The collab UI path (1004+) is
   unchanged. Button props: `status={supabaseSync.status}`, `lastSyncedAt={supabaseSync.lastSyncedAt}`,
   `error={supabaseSyncStatus.error}`, `onSyncNow={supabaseSync.syncNow}`,
   `isSignedIn={!!supabaseUserId}`, `onRequestSignIn={() => setIsSignInOpen(true)}`.
5. **SignInDialog — lines 1103-1108** (flag-gated, right after `<ShareDialog>`):
   `{isSupabaseSyncEnabled() && (<SignInDialog open={isSignInOpen} onClose={() => setIsSignInOpen(false)} />)}`.

### renderTopRightUI dependencies
`renderTopRightUI` is a **plain inline arrow** passed to `<Excalidraw renderTopRightUI={...}>` — it is
NOT wrapped in `useCallback`, so it is recreated every render and its closure always captures the
latest `supabaseSync`, `supabaseUserId`, `supabaseSyncStatus`, and `setIsSignInOpen`. No dep array
exists, so there is no stale-closure risk and `react-hooks/exhaustive-deps` has nothing to flag
(confirmed: `eslint excalidraw-app/App.tsx` exits 0). No `// eslint-disable` was needed.

### Verify evidence (from repo root, after edits)
- `yarn test:typecheck` → `$ tsc` / `Done in 6.23s.` (PASS; baseline was also clean.)
- `yarn eslint excalidraw-app/App.tsx` → `Done in 1.50s.` and `node_modules/.bin/eslint
  excalidraw-app/App.tsx` → `RAW_ESLINT_EXIT=0` (zero findings).
- `yarn vitest run excalidraw-app/tests` →
  ```
  Test Files  7 passed (7)
       Tests  30 passed (30)
  ```
  Identical to the pre-edit green baseline (30/30). Files: `supabase/syncEngine.test.ts` (11),
  `supabase/boardRepository.test.ts` (7), `supabase/supabaseFiles.test.ts` (4),
  `supabase/useSupabaseSync.test.tsx` (3), `MobileMenu.test.tsx` (2), `collab.test.tsx` (2),
  `LanguageList.test.tsx` (1). No NEW failures. (Pre-existing stderr noise: "Error JSON parsing
  firebase config" + punycode DeprecationWarning — present in baseline, not failures.)

### Flag-off behavior unchanged
`isSupabaseSyncEnabled()` is `import.meta.env.VITE_APP_FEATURE_SUPABASE_SYNC === "true"`, unset in
tests → false. Every added code path is gated: the hook no-ops (no engine constructed),
`renderTopRightUI` falls through to the unchanged `LiveCollaborationTrigger` path, `onChange` skips
`notifyChange()`, and `<SignInDialog>` is not rendered. The dedicated flag-on App-level render assertion
lives in T11's `tests/supabase-sync.test.tsx` (not part of T09; that file does not yet exist).

### Constraints honored
Edited ONLY `excalidraw-app/App.tsx`. Did NOT touch `AppMainMenu.tsx`, `AppWelcomeScreen.tsx`,
`data/index.ts`, or any hook/component/data-layer file (those are T10). No `as const satisfies`.
Prettier style matched (verified via eslint, which includes prettier rules).
