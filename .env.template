# Dieses Template listet alle für die Anwendung benötigten Umgebungsvariablen.
# Kopieren Sie diese Datei nach .env und füllen Sie die Werte aus.
# Die .env-Datei wird von Git ignoriert und darf niemals eingecheckt werden.

# Beispiel für Firebase-Konfiguration (wird für Meilenstein 7 benötigt)
VITE_FIREBASE_API_KEY=
VITE_FIREBASE_AUTH_DOMAIN=
VITE_FIREBASE_PROJECT_ID=
VITE_FIREBASE_STORAGE_BUCKET=
VITE_FIREBASE_MESSAGING_SENDER_ID=
VITE_FIREBASE_APP_ID=
