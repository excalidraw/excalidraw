# Supabase sync (self-hosted). Use the PUBLIC anon key only — never the service_role key.
VITE_APP_SUPABASE_URL=
VITE_APP_SUPABASE_ANON_KEY=
# Enable the Supabase sync feature (disables live collaboration + share links when "true")
VITE_APP_FEATURE_SUPABASE_SYNC=true
