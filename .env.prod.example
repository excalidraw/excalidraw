# Use a strong URL-safe value (letters, numbers, _ and -) because it is also
# interpolated into DATABASE_URL.
POSTGRES_PASSWORD=replace-with-a-strong-url-safe-password
GCS_BUCKET=your-bucket-name
HTTP_PORT=80
