# Golden plan fixtures

Committed canonical exports for fast CI. Regenerate from `.corpus/` after intentional importer changes:

```bash
cp ../.corpus/case-098/plan.json allplanmodules.json
cp ../.corpus/case-098/graph.dot allplanmodules.dot
```

Or copy from legacy root-level `allplanmodules.*` until the corpus replaces them.
