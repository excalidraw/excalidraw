import { KEYS } from "@excalidraw/common";

import { Excalidraw } from "../..";
import { Keyboard } from "../../tests/helpers/ui";
import { act, render } from "../../tests/test-utils";

describe("FontPickerList - Feature Itálico", () => {
    // Tarefa 1
    it("deve exibir a opção Itálico no FontPickerList ao selecionar a ferramenta de texto", async () => { ... });
// Tarefa 2
it("deve permitir selecionar a fonte Itálico e refleti-la como ativa na UI", async () => { ... });
// Tarefa 3
it("deve criar um elemento de texto com a propriedade fontFamily configurada para Itálico (10)", async () => { ... });
// Tarefa 4
it("deve renderizar o ícone FontFamilyItalicIcon na opção de Itálico", async () => { ... });
// Tarefa 5
it("deve conseguir alterar a fonte de um texto já existente para Itálico", async () => { ... });
});